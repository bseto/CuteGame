#include "gamestats.h"

/*
void GameStats::updateScore() {
    score_++;
    ui->ScoreCount->setText(QString::number(score_));
}

void GameStats::updateClicks() {
    clicks_++;
    updateAccuracy();
    ui->ClicksCount->setText(QString::number(clicks_));
}

*/
